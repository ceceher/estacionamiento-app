type Cars @model @auth(rules: [{allow: public}]) {
  id: ID!
  placas: String!
  modelo: String!
  color: String!
  fechaEntrada: AWSDate
  horaEntrada: AWSTime
  fechaSalida: AWSDate
  horaSalida: AWSTime
}
 