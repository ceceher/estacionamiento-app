type Cars @model @auth(rules: [{ allow: public }]) {
  id: ID!
  placas: String!
  modelo: String!
  color: String!
  fechaEntrada: AWSDateTime
  fechaSalida: AWSDateTime
  status: String
  costo: Float
}
